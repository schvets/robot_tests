language: python # set desired language

dist: trusty

system_site_packages: false

python:
 - "2.7"

addons:
  apt:
    sources:
      - google-chrome # if your project contains tests need to be run on chrome include these lines
    packages:
      - google-chrome-stable

install:
  - sudo apt-get install python-minimal python2.7-dev git gcc libyaml-dev libjpeg-dev libz-dev
  - ls

script:
  # perform build and run commands here
  - python bootstrap.py
  - python bootstrap.py
  - ls
  - cd eggs
  - rm -rf zc.buildout-2.10.0-py2.7.egg
  - ls
  - cd ..
  - bin/buildout -N
  - bin/buildout -N
  - cd eggs
  - ls
  - cd ..
  - bin/op_tests -s 1_openProcedure -As_arguments/openeu_testing.txt -v  broker:PrivatMarket -v role:viewer -v api_version:2.4 || true  - ls
  - cd bin
  - ls